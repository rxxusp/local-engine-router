# All of these are already present in the system Python 3.12 on this box.
# Listed for reproducibility / fresh installs (pip install -r requirements.txt).
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pyyaml>=6.0
psutil>=5.9
